// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig:{
    apiKey: 'AIzaSyClPRQqVYrRse7I-CybK5dsFrGHyhdNE6E',
    authDomain: 'bookstore2-549dc.firebaseapp.com',
    databaseURL: 'https://bookstore2-549dc.firebaseio.com',
    projectId: 'bookstore2-549dc',
    storageBucket: 'bookstore2-549dc.appspot.com',
    messagingSenderId: '353980490853',
    appId: '1:353980490853:web:2f7a04abc00c0ab279928c'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
